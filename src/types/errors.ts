const errors: any = {
  NOACTION: "No actions were added to the app",
  NOTRIGGER: "No triggers were added to the app",
  INVALID_DISCORD_WEBHOOK: "Invalid Discord webhook URL",
  INVALID_SLACK_WEBHOOK: "Invalid Slack webhook URL",
  INVALID_TELEGRAM_WEBHOOK: "Invalid Telegram webhook (must be a bot)",
  INVALID_DESTINATION: "Invalid destination for notification",
  NOKEY: "No API key was provided",
  APIERROR: "An error occurred while communicating with the API",
};

export default errors;
